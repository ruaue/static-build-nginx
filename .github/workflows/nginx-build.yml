name: nginx static build
permissions:
  contents: write
on:
  workflow_dispatch:
  
jobs:
  build:
    runs-on: ubuntu-latest
    container: 
      image: ruby:alpine
      env:
        TZ: Asia/Shanghai
      options: --privileged
      
    steps:

    - name: git clone static build alpine
      run: |
        set -eux
        apk --update add build-base musl-dev
        apk add git
        git clone   https://github.com/ruaue/static-build-nginx
         
         
    - name: install bash
      run: |
       apk --update add bash

    - name: start build by script
      run: |
        bash nginx.sh

    - name: setup fpm    
      run: |
       gem install fpm
       fpm --version
       
    - name: ls 
      run: |
         pwd
         ls /usr/sbin/nginx
         ldd usr/sbin/nginx
         
    - name: Upload release
      uses: softprops/action-gh-release@v0.1.15
      with:
       tag_name: ${{ github.ref_name }}
       files: |
           /usr/sbin/nginx
